
name: Build

on:
  push:
    branches:
      - main
  repository_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v2
        with:
          token: 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
          submodules: true
      - uses: actions/setup-node@v2
          
      - run: npm install
      - uses: cloudflare/wrangler-action
        with:
          apiToken: L8ejzU1KbWspHLp6JIR1L5ZXy2Nh3uX1Gq2xXf-c
          secrets: |
            FAUNA_DB_KEY
        env:
          FAUNA_DB_KEY: fnAEVrP_-SACTG23SNcQEeolY5spCGGfyTk9E2XA
        preCommands: |
          wrangler kv:namespace create KV_STATUS_PAGE
